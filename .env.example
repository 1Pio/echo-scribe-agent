# (optional) Development Settings
LOG_LEVEL=INFO
DEBUG_MODE=false

# (optional) LiveKit worker connection (required even for console mode) :contentReference[oaicite:5]{index=5}
# LIVEKIT_URL=ws://127.0.0.1:7880
# LIVEKIT_API_KEY=devkey
# LIVEKIT_API_SECRET=devsecret

# (optional) Local model endpoints
# OLLAMA_BASE_URL=http://127.0.0.1:11434/v1
# LOCAL_STT_BASE_URL=http://127.0.0.1:8001/v1
# KOKORO_BASE_URL=http://127.0.0.1:8880/v1

# (optional) Service Daemon
# SERVICE_DAEMON_HOST=127.0.0.1
# SERVICE_DAEMON_PORT=8787
# SERVICE_LEASE_TTL_S=15
SERVICE_IDLE_TIMEOUT_S=25
SERVICE_STARTUP_TIMEOUT_S=25
SERVICE_WAIT_READY_S=90
SERVICE_DAEMON_LOG_DIR=./.logs  # enable if you want stt.log written

# (optional) Model Choices
# OLLAMA_MODEL=qwen3:8b
KOKORO_VOICE=af_heart
LOCAL_STT_MODEL=whisper-1

# (optional) OBSIDIAN Journal Folder
OBSIDIAN_JOURNAL_FOLDER=C:\Users\aaron\iCloudDrive\iCloud~md~obsidian\OBSIDIAN-vault\Daily


# Performance Tuning (Optional)
# These variables fine-tune the agent's performance characteristics

# LLM temperature (0.0-1.0, lower is faster/more deterministic)
# Default: 0.7 (optimized for voice assistant use case)
LLM_TEMPERATURE=0.7

# STT health check timeout in seconds
# Default: 30.0
STT_HEALTH_TIMEOUT=30.0

# STT health check interval in seconds
# Default: 0.5
STT_HEALTH_CHECK_INTERVAL=0.5

# Prewarm worker count for production deployment
# Default: 1 (increase to 3-5 for production to handle concurrent sessions)
PREWARM_COUNT=1
